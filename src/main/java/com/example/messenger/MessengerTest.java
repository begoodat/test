package com.example.messenger;
/*
public class MessengerTest {
    public static void main(String[] args){
        IPhoneMessenger iPhoneMessenger = new IPhoneMessenger();
        GalaxyMessenger galaxyMessenger = new GalaxyMessenger();

        System.out.println("메신저 최소 문자 크기" + Messenger.MIN_SIZE);
        System.out.println("메신저 최대 문자 크기" + Messenger.MAX_SIZE);

        iPhoneMessenger.setLogin(true);
        iPhoneMessenger.getMessage();
        iPhoneMessenger.setMessage("hello");
        iPhoneMessenger.clearMessage();

        galaxyMessenger.setLogin(true);
        galaxyMessenger.getMessage();
        galaxyMessenger.setMessage("hi");
        galaxyMessenger.changeKeyboard();



    }
}
*/

        /*
        인터페이스 본문을 갖지 않는 추셍 메소드에 대해서는 반드시 오버라이딩하여 본문을 구현해야하는 의무가 있다.
        인터페이스를 만들고 비슷한 기능을 처리하는 곳에서 인터페이슬 상속하여 구현하면 일관된 구조를 가질 수 있고 유지보수가 수월해진다.
         */